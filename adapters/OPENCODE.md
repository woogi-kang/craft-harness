# Craft Harness Adapter: OpenCode

Craft Harness provides reusable skills, commands, and orchestration conventions.

Recommended checks:

```bash
./craft doctor
./craft validate
./craft catalog --format json --output /tmp/craft-catalog.json
```

When changing code, prefer small diffs, run local verification, and report the
exact command results in the final response.
