# Project Initialization

Before converting Figma to Flutter, inspect the existing project. If no Flutter
project exists, initialize one with the standard Flutter CLI and record the
created paths in the worker handoff.

This public placeholder keeps legacy agent links stable. The detailed workflow
will move into the Dev pack registry as part of the v0.2 cleanup.
